pipeline {
  agent any
    tools{
          maven 'maven3'
         }
       stages{
          stage('Building the code') {
          Steps{
              echo "build is in process"
              echo ${M2_HOME}
              echo ${PATH}
               }
              }
       }


}
